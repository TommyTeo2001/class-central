'use strict';

/**
 * classroom-enrollment router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::classroom-enrollment.classroom-enrollment');
