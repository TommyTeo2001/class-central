'use strict';

/**
 * general-translation controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::general-translation.general-translation');
